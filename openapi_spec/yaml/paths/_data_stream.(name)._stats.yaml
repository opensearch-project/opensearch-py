---
get:
  x-endpoint-group: indices.data_streams_stats
  description: Provides statistics on operations happening in a data stream.
  parameters:
  - in: path
    name: name
    description: A comma-separated list of data stream names; use `_all` or empty
      string to perform the operation on all data streams
    required: true
    schema:
      $ref: "../schemas/_common.yaml#/string_array"
